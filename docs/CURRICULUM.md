# AI 기초 4일 과정 — 슬라이드

초심자(코딩·AI 첫 경험) 대상. 하루 8시간 × 4일 = 32시간.
데이터셋은 **합성 양극재 소성 공정 데이터** 하나로 나흘을 관통한다. 열 이름은 한글이다. 회사명은 넣지 않는다.

| 덱 | 파일 | 슬라이드 |
|---|---|---|
| Day 1 · 파이썬 문법 | [day1/index.html](day1/index.html) | 60 |
| Day 2 · 데이터 다루기 | [day2/index.html](day2/index.html) | 48 |
| Day 3 · 머신러닝 | [day3/index.html](day3/index.html) | 49 |
| Day 4 · 딥러닝 | [day4/index.html](day4/index.html) | 26 |

조작: `← →` 또는 Space · 숫자 입력 후 Enter로 점프 · `≡` 목차 · `−/+` 줌 · PDF 버튼(인쇄).

---

## 챕터 구성

```
Day 1  파이썬 문법
  CH0 작업 환경         두 가지 실행 환경 · 로컬(Anaconda·가상환경·Jupyter) · 클라우드(Colab·세션·GPU) · 고르는 기준
  CH1 값과 변수         자료형 · 연산자 · 문자열(인덱스·f-string·메서드 표) · 불변과 가변 · 얕은 복사와 깊은 복사
  CH2 자료 구조         리스트 · 튜플 · 집합 · 딕셔너리 · 중첩 · 비교표 · 메서드 표
  CH3 흐름 제어         입력 · 형변환 · 비교와 논리 · 조건문 · elif · for · enumerate · while · 컴프리헨션 4종
  CH4 함수              입력·처리·출력 · 내장 함수 · def · 반환 · 인자의 가변성 · 메서드

Day 2  데이터 다루기
  CH0 NumPy             배열 · dtype · shape · 인덱싱 · 마스크 · reshape · 브로드캐스팅 · 행렬 곱
  CH1 Pandas            쓰는 이유 · DataFrame · read_csv · 열 선택 · 행 필터링 · loc · iloc · 타입 · 시리즈 연산 · groupby
  CH2 전처리            결측 · 인코딩 · 스케일링 · 훈련/테스트 분할 · 전처리 순서
  CH3 시각화            Matplotlib · 제목과 축 이름 · 범례 · 기본 네 그래프 · subplot · 한글 폰트 · seaborn

Day 3  머신러닝
  CH0 학습의 뼈대       규칙의 출처 · 세 갈래 · 지도/비지도/강화 · 회귀 · 분류
  CH1 학습의 원리       선형 회귀 · 손실 함수 · 기울기 · 경사 하강법 · 학습률
  CH2 모델              공통 네 줄 · KNN · 로지스틱 회귀 · SVM · 결정 트리 ·
                        선형 회귀 · 다항 회귀 · 규제(Ridge·Lasso) · 기법 비교
  CH3 앙상블            앙상블 · 배깅 · 랜덤 포레스트 · 부스팅 · 배깅↔부스팅 · XGBoost
  CH4 검증과 평가       과적합 · 검증셋(3분할) · K-Fold · 하이퍼파라미터 탐색 · 모델 비교 ·
                        혼동 행렬 · 지표 선택 · 클래스 불균형 · 새 배치 판정 · 모델 저장

Day 4  딥러닝
  CH0 객체지향 기초     클래스 · 인스턴스 · 상속과 재정의
  CH1 신경망 구조       퍼셉트론과 XOR · 은닉층 · 활성화 함수 · 순전파 · shape
  CH2 PyTorch           텐서 · nn.Module · 학습 루프 다섯 줄 · 역전파 · 옵티마이저
  CH3 실전              에러 다섯 · shape 에러 · 점검 순서 · 미니 프로젝트
```

시각은 슬라이드에 넣지 않는다. 배분은 강사가 현장에서 조절한다.

각 덱은 **표지 → 오늘의 일정 → 첫 챕터**로 바로 들어간다. 과정 구조를 설명하는 슬라이드는 없다.

---

## 초안 대비 바꾼 것

받은 4일 초안에서 **순서 세 곳을 고치고, 빠진 것을 채우고, 역전파를 줄였다.**

### 순서

| 초안 | 바꾼 것 | 이유 |
|---|---|---|
| 자료형 → 곧바로 OOP | 자료형 → **조건·반복·함수** → (Day 4) OOP | `def`를 모르는 상태에서 `class`·`__init__`·`self`는 안 붙는다. OOP는 `nn.Module`이 눈앞에 있는 Day 4로 옮겼다 |
| List Comprehension을 `for` 전에 | `for` → `range` → `while` → **컴프리헨션** | 접기 전에 펴진 형태를 먼저 본다 |
| 미분(Day 2) ↔ 경사 하강법(Day 4) | 둘을 **Day 3 오전에 붙였다** | 이틀 간격이면 잊는다. 기울기를 배운 직후 바로 굴려 본다 |
| NumPy를 Day 2 맨 뒤에 | **배열·shape·마스크는 Pandas 앞**, reshape·브로드캐스팅·행렬 곱은 뒤 | Series 내부가 NumPy 배열이다. 뒤에 두면 `df[df['Age'] < 18]`을 원리보다 여덟 장 먼저 쓰게 된다. 뒤 묶음은 Day 4의 `Wx + b`로 바로 이어진다 |

### 채운 것

| 추가 | 어디 | 없으면 |
|---|---|---|
| 조건문 · 반복문 · 함수 | Day 1 CH4·CH5 | 초안에 통째로 없었다. 나머지가 전부 공중에 뜬다 |
| **Pandas** | Day 2 CH1 (17장) | 초안에 없는데 Day 4 과제가 "Tabular 전처리"였다. CSV를 NumPy로 전처리할 수는 없다 |
| **전처리** — 결측·인코딩·스케일링 | Day 2 CH2 (9장) | 스케일링을 빼면 Day 4에서 **손실이 안 떨어진다**. 실습 실패의 직접 원인 |
| **시각화** | Day 2 CH3 (6장) | 손실 곡선을 못 그리면 학습 성공 여부를 확인할 수단이 없다 |
| **과적합 · 교차 검증** | Day 3 CH3 | 초안은 train/test split만 있었다. 훈련 정확도만 보고 보고하게 된다 |
| **평가 지표 확장** | Day 3 CH3 | 초안은 "Accuracy, MSE 등". 불균형 데이터에서 정확도가 무의미하다는 것을 스팸 예제(97%)로 보인다 |
| **에러 읽는 법** | Day 4 CH3 (2장) | shape 에러는 PyTorch 실습에서 100% 만난다 |
| **로컬 환경 세팅** | Day 1 CH0 (4장) | Anaconda 설치 · 가상 환경 · Jupyter Lab. 사내 데이터는 밖으로 못 내보내므로 로컬 경로가 필요하다 |
| **불변과 가변** | Day 1 #21~23, #52 | 요청 반영. 아래 별도 항목 |
| 하루 끝 워크타임 | 전 일 | 참고 교안에는 있었는데 초안에서 빠졌다. 완주율을 가른다 (슬라이드에는 표기하지 않는다) |

### 줄인 것

| 초안 | 바꾼 것 | 근거 |
|---|---|---|
| 역전파 이론 3h + Autograd 2h | **1.5h** — 연쇄 법칙 그림 1장 + `backward()` 3줄 | 이 청중은 역전파를 직접 구현하지 않는다. SGD/Adam 비교도 한 장으로 줄였다 |
| 수학 3h 연속 강의 | **2h, 코드 확인형** | 기울기를 수치미분 코드로 직접 재 본다 |
| Colab + VS Code | **Anaconda + Jupyter · Colab 둘 다** | VS Code 는 뺐다. 로컬(Anaconda·Jupyter)과 클라우드(Colab)를 양쪽 다 다룬다 |
| 입과식 1.5h | **1h** | |

### 뺀 것 — 커리큘럼 안내

과정 구조를 설명하는 슬라이드를 전부 없앴다. 무엇을 배울지, 지난 회차에 무엇을 했는지는
**강사가 말로 한다.** 슬라이드에 넣으면 아직 안 배운 개념이 앞으로 끌려 나온다.

| 없앤 것 | 이유 |
|---|---|
| Day 1 CH0 챕터 5장 (전체 흐름 · 나흘 동안 하는 일 · 1주차와 2주차의 경계 · AI가 하는 일 · 에러 없이 돌아가는 오답) | 첫 시간에 `groupby`·회귀·LLM이 이름부터 나온다. 배울 준비가 안 된 청중에게 소음이다 |
| Day 4 나흘 정리 · 2주차 로드맵 2장 | 전체 회고와 다음 주 예고. Day 4 내용만 남긴 정리 한 장으로 대체했다 |
| 각 덱의 `다음` keymsg 4곳 | `Day 2는 …를 다룬다` 같은 예고 |
| 회차 상호 참조 60곳 | `Day 2의 get_dummies` → `get_dummies`. 회차 번호 대신 개념 이름으로 부른다 |
| 시각 표기 전부 | `17:00–18:00 워크타임`, `09:00 · CH0`, `15:00 – 17:30` |
| 표지의 `.lead` · `.meta`, 간지의 설명문 `<p>` | 기준 덱은 표지가 `kick + h1`뿐이고 간지에 설명문이 없다 |

lint가 이 여섯 가지를 전부 막는다 — `회차 언급`, `시간 표기`, `제목 서술문` 규칙.

---

## 불변과 가변

요청하신 항목이라 세 장으로 나눠 넣었다. 원시 타입 / 참조 타입이라는 다른 언어의 용어는
쓰지 않는다. 파이썬은 전부 객체이므로 **가변성**만으로 설명이 끝난다.

| # | 제목 | 내용 |
|---|---|---|
| 21 | 값이 바뀌는 자료형과 바뀌지 않는 자료형 | `int/float/str/tuple` vs `list/dict/set`. `b = a` 후 결과가 갈리는 코드 두 개 |
| 22 | 대입과 복사의 차이 | `.slots` 메모리 도해 + `id()`로 확인. `b = a`는 복사가 아니라 이름을 더 붙이는 것 |
| 23 | 얕은 복사와 깊은 복사 | 중첩 구조에서 `.copy()`가 안 통하는 코드 + `copy.deepcopy()`. 3행 비교표 |
| 52 | 함수 인자와 가변성 | `add_one(x)`는 밖이 안 바뀌고 `append_one(lst)`는 바뀐다 |

---

## 데이터셋

**합성 이차전지 양극재 소성 공정 데이터** 1412행 11열. 나흘 내내 같은 표를 쓴다.
실제 데이터가 아니라 [`data/make_dataset.py`](../data/make_dataset.py)가 난수에서 만든 것이고,
시드가 고정돼 있어 언제 돌려도 같은 파일이 나온다. 회사·설비·제품을 특정하지 않는다.

```python
url = 'https://tunalee.github.io/posco/data/batch_quality.csv'
df = pd.read_csv(url, thousands=',', na_values=['N/A', '-'])   # 열 이름은 한글
```

열 정의와 심어 둔 문제는 [docs/data/README.md](data/README.md)에 있다.

가르칠 것을 일부러 심어 뒀다.

- **결측** — `입도` 112건, `수분율` 73건. 표기가 `N/A` `-` `''`로 흔들린다
- **타입 사고** — `불순물`가 `'1,240'` 쉼표 탓에 `object`로 읽힌다. `.max()`가 `'998'`을 낸다
- **표기 흔들림** — 같은 설비가 `'A'` `'a'` `' A '`로 24종이 된다
- **이상치** — `성형압력`의 센서 오류값 `999.0` 18건
- **중복** — 같은 배치가 두 번 기록된 12행
- **불균형** — 불량 18.8%. 전부 양품이라 찍으면 정확도 81%가 나온다
- **무관한 열** — `성형압력`는 용량과 상관 −0.05다

`방전용량`가 회귀 정답, `양품여부`가 분류 정답이다. 온도는 890 °C 부근이 최적이고
대부분의 배치가 그 아래에 있어, **선형 모델도 어느 정도 맞히고 트리 계열이 더 잘 맞힌다.**

| 모델 | 분류 정확도 |  | 회귀 | R² |
|---|---|---|---|---|
| 전부 양품이라 찍기 | 0.810 | | 선형 회귀 | 0.719 |
| 로지스틱 회귀 | 0.896 | | 랜덤 포레스트 | 0.837 |
| 결정 트리 | 0.922 | | | |
| 랜덤 포레스트 | 0.944 | | | |

`line_master.csv`(6행)를 `merge`하면 **설비가 오래될수록 불량률이 높다**는 것이 드러난다 —
2015년 설비 21.4%, 2021년 16.0%.

Day 3의 평가 지표 전체(혼동 행렬 · 정확도의 함정 · 정밀도/재현율 · 지표 선택)가
불량 검출 위에서 돈다. 놓친 불량은 고객 클레임, 헛경보는 라인 정지 — 비용이
비대칭이라 지표 선택을 설명하기에 맞다.

**회사명은 넣지 않는다.** 공개 저장소이므로 업종 일반 용어만 쓴다.

---

## 미니 프로젝트

빈 노트북이 아니라 **스켈레톤에 `# TODO` 8곳**을 채우는 방식이다.

```
TODO 1  CSV 읽기            TODO 5  train_test_split
TODO 2  안 쓸 열 제거        TODO 6  StandardScaler
TODO 3  결측 처리            TODO 7  MLP 클래스 완성
TODO 4  원-핫 인코딩         TODO 8  학습 루프 + 손실 곡선
```

제출물은 GitHub `.ipynb` 링크 하나. 채점 기준은 점수가 아니라 **재현성**이다 —
`런타임 초기화 후 모두 실행`으로 끝까지 도는지.

---

## 검증

| 항목 | 결과 |
|---|---|
| 문체 lint (제목 명사구 · 시각 표기 규칙 포함) | day1 0 · day2 0 · day3 0 · day4 0 |
| 1280×720 넘침 | 4개 덱 모두 0장 — `scripts/overflow.cjs`, 웹폰트 로딩 후 실측 |
| 에셋 참조 | day1 4/4 (Colab 캡처) · day3 3/3 (학습 방법 도해) · 나머지는 인라인 SVG |

하네스: `~/.claude/skills/lecture-deck/` — `scripts/lint.py`, `assets/template.html`,
`references/voice.md`(문체 규칙), `references/components.md`(컴포넌트 카탈로그).

표지는 `kick + h1`뿐이고 간지에 설명문을 넣지 않는다. 시각 표기는 lint가 막는다.

pptx 기반 이전 덱은 [_archive-pptx/](_archive-pptx/)에 보관돼 있다.
